# Required
RPC_ENDPOINT=https://evmrpc-testnet.0g.ai
# Optional
FL_ENC_MODE=dev
# Storage (set when enabling real uploads)
OG_STORAGE_MODE=ipfs-api
OG_STORAGE_API_BASE=https://ipfs.infura.io:5001
OG_GATEWAY_BASE=https://infura-ipfs.io/ipfs
# Secret: set OG_STORAGE_API_TOKEN only in local .env or GH Secrets
